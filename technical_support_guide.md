# 🛠️ מדריך תמיכה טכנית ומענה לתקלות - מערכות תחנה ופריוריטי

מדריך ניווט ותפעול מלא לתומך טכני. מותאם להעלאה ל-**GitHub** עם תוכן עניינים מקושר, טבלאות קודים, נתיבי קבצים, ותרשימי זרימה לתקלות.

---

## 📋 תוכן עניינים
1. [📱 מערכת פריוריטי (Priority CRM)](#1-מערכת-פריוריטי-priority-crm)
2. [💻 תוכנות השתלטות מרחוק](#2-תוכנות-השתלטות-מרחוק)
3. [⛽ הכרת התחנה ומערכת RFC3000](#3-הכרת-התחנה-ומערכת-rfc3000)
4. [📁 סייר הקבצים Salamander ונתיבי dda](#4-סייר-הקבצים-salamander-ונתיבי-dda)
5. [🔍 פענוח לוגים וטיפול בהפרשים בדוחות](#5-פענוח-לוגים-וטיפול-בהפרשים-בדוחות)
6. [⚙️ מערכת תצורה וקונפיגורציה](#6-מערכת-תצורה-וקונפיגורציה)
7. [💳 מערכת שב"א וסליקת אשראי](#7-מערכת-שבא-וסליקת-אשראי)
8. [📊 ניהול מלאי דלקים](#8-ניהול-מלאי-דלקים)
9. [🔧 חיוב / זיכוי אשראי יזום](#9-חיוב--זיכוי-אשראי-יזום)
10. [🚨 תקלות נפוצות ופתרונן](#10-תקלות-נפוצות-ופתרונן)
11. [🖥️ מסלקות (DDA / DDM / POS)](#11-מסלקות-dda--ddm--pos)
12. [🔄 תרשים טיפול בפוס תקוע](#12-תרשים-טיפול-בפוס-תקוע)

---

## 1. 📱 מערכת פריוריטי (Priority CRM)

### 🔑 כניסה ופתיחת קריאה
* **כניסה למערכת:** הקלדת שם משתמש + סיסמה בעת העלייה.
* **פתיחת קריאה חדשה:** `תפריט אישי` ➔ `קריאות שירות` ➔ לחיצה על **F6** לחיפוש לקוח.
* **הוספת תיאור/הערה:** `תיאור תקלה` ➔ לחיצה על החלק הלבן התחתון ➔ `פתיחת קריאה` ➔ `כן`.
* **עבור מדף פתיחת תקלה:** מקש קיצור `Ctrl + Enter`.

### 🔍 חיפוש קריאות והיסטוריה
* **כל הקריאות הפתוחות:** `תפריט אישי` ➔ `לוח קריאות שירות פעילות` ➔ בחירת התור המבוקש בחלון העליון.
* **היסטוריית קריאות לתחנה:** `תפריט אישי` ➔ `קריאות שירות` ➔ **F11** ➔ הקלדת `*` ➔ חיפוש התחנה ➔ גלילה ימינה לצפייה בהיסטוריה.

### 👤 ניהול פרטי משתמש והודעות
* **שינוי פרטי משתמש:** `תפריט אישי` ➔ `לקוחות` ➔ הקלדת `*` ➔ חיפוש משתמש (או לחיצה כפולה על **F6** בזמן פתיחת קריאה).
* **הגדרת הודעה קופצת בעת פתיחת לקוח:**
  1. מעבר לפרטי משתמש.
  2. הזנת הטקסט בשדה `הודעה בעת הקלדת לקוח`.
  3. לחיצה על `Ctrl + Z` (לשמירה וחזרה לפתיחת קריאה).

### 🛠️ פתיחת קריאה לטכנאי
* **קוד קבוצה:** `200` (טכנאים)
* **קוד תקלה:** נושא התקלה המבוקש
* **סוג קריאה:** `תמיכה מרחוק` ➔ `טכנאים` *(מייל נשלח אוטומטית לטכנאים)*.

### 📱 שליחת הודעת SMS לטכנאי
* כניסה לתוכנת ה-SMS באינטרנט.
* **נוסח:** `שם תחנה` + `פירוט הקריאה` + `מספר טלפון` + `מספר קריאה`.
* **לשונית קבוצות** ➔ `בחר את כולם` ➔ `שלח SMS`.

### 🔄 עדכון סטטוס והעברת קריאות
* **שינוי סטטוס:** לשונית `ביצוע` ➔ בחירה ב-`מבוטלת` או `סגורה`.
* **שינוי סוג קריאה:** מילוי קוד קבוצה + קוד לקוח ➔ `סוג קריאה` ➔ בחירה ב-`טכנאים` / `תמיכה`.
* **העברה בין תומכים:** לשונית `זמנים ושיבוץ` ➔ `טכנאי מטפל` ➔ בחירת התומך המבוקש.

---

## 2. 💻 תוכנות השתלטות מרחוק

### 🔴 Netop Remote Control
* **נתיב הפעלה אצל התומך:** `התחל` ➔ `All Programs` ➔ `Netop Remote Control` ➔ `Guest`.
* **הגדרת תחנה ב-Phonebook:**
  * קליק ימני ➔ `New` ➔ `Phone Entry`.
  * `Description`: שם הלקוח / תחנה.
  * `Name`: כתובת IP של התחנה.
  * `Quick Connection`: כתובת IP.

#### סיסמאות ברירת מחדל ב-Netop:
| רשת / סוג תחנה | סיסמה |
| :--- | :--- |
| **דור אלון** | `1-4` |
| **טן** | `1-6` |
| **בקרים (תחנות ציבוריות)** | `1-8` |

#### סרגל כלים ופעולות ב-Netop:
* 🖱️ **נעילת עכבר ומקלדת (אייקון עכבר):** נועל את קלט המשתמש בתחנה.
* 📋 **העתקת טקסט/נתונים:** שימוש ב-`Send Keyboard` או `Retried`.
* 💬 **Chat:** פתיחת חלון צ'אט מול המשתמש בתחנה.
* 🛑 **Remote Control:** יציאה מהשתלטות.
* 📁 **File Transfer:** חלון העברת קבצים (ימין = תחנה מרוחקת, שמאל = מחשב תומך). העברה מתבצעת בגרירה.
* 📁 **Make New Folder:** יצירת תיקייה חדשה בתחנה.

#### הפעלת Netop Host ע"י הלקוח (במידה ונופל):
* `שב"א` ➔ `Netop`
* או: `התחל` ➔ `All Programs` ➔ `Netop Remote Control` ➔ `Host`.

### 🔵 TightVNC Viewer
* אצל התומך: **Viewer** | אצל הלקוח: **Server**.
* **סיסמה נפוצה:** `1-4`.

### 🟢 TeamViewer
* משמש בעיקר לתחנות עם חיבור אינטרנט חיצוני / סטיק (בקרים).

---

## 3. ⛽ הכרת התחנה ומערכת RFC3000

ניתן לגשת לממשק הניהול דרך Netop בלחיצה על **RFC3000**:

* **משמרות:** קליק ימני ➔ `פתיחת משמרת` / `סגירת משמרת`.
* **הפקת דוחות:** `ניהול` ➔ `דוחות` ➔ בחירת קופה (1/2) ➔ `הצג` (ניתן לבחור סוג דוח).
* **הדפסת עסקה בודדת:** `הצג הדפסת עסקה` ➔ בחירת העסקה (מדפיס מדפסת התחנה).
* **אבחון הפרשים:** הפרש מופיע בערך **אדום**.
* **דוח חודשי / תקופתי:**
  * ניתן לחזור אחורה עד **חצי שנה**.
  * ביטול סימון דוח חודשי: קליק ימני ➔ `בחר את כל המשמרות` ➔ `ביטול`.
* **קונפיגורציית תחנה:** `ניהול` ➔ `תצורה` (סיסמה: `100` או `22`).
  * **הוספה/הסרת מתדלק:** קוד עובד מתחיל ב-`07` + 4 ספרות מס' תחנה.
  * **עדכון מחירים והנחות:** בחירת תאריך לשינוי מחירים והגדרת הנחות לפי סוג דלק.
  * **מאסטר-שלטים:** הפעלת עמדה באמצעות שלטים.
  * **קבלות:** עריכת פרטי התחנה בכותרת/סיומת הקבלה.

---

## 4. 📁 סייר הקבצים Salamander ונתיבי dda

סייר הקבצים **Salamander** משמש לניהול קבצים, לוגים ודוחות בכונן `C:`.

### ⌨️ מקשי קיצור ב-Salamander
| מקש | פעולה |
| :---: | :--- |
| **F2** | שינוי שם קובץ (Rename) |
| **F5** | העתקת קובץ/תיקייה (Copy) |
| **F6** | העברת קובץ/תיקייה (Move) |
| **F7** | יצירת תיקייה חדשה (New Folder) |
| **Ctrl + F** | חיפוש קובץ (Search) |
| **Ctrl + F12** | סינון קבצים (Filter) |

### 📂 נתיבי מערכת קריטיים (`C:/dda`)
כל קבצי ה-EXE והלוגים התפעוליים ממוקמים בתיקייה `C:/dda`:

```text
C:/dda/
├── station/
│   ├── data/             <-- דוחות פירוט משמרות (סגירות, פתיחות, עסקאות)
│   ├── log/              <-- קבצי לוג לפי משאבות (dlgDDMM.0301)
│   ├── prices/           <-- קבצי מחירונים והנחות (prices.dat, discount.dat)
│   ├── 4upload/          <-- קבצי לוג מוכנים למשיכה
│   ├── shva/             <-- קבצי תקשורת אשראי (tran)
│   └── ch.prm            <-- הגדרות כרטיסי עובדים ושלטים
```

### 📄 קבצי דוחות תדלוק ומוצרים
* **קובץ RS (תדלוקים):** מכיל את כל עסקאות התדלוק שבוצעו במשמרת.
  * *מבנה שם הקובץ:* `Rs060418.d1a`
    * `06` = יום | `04` = חודש | `18` = שנה | `d` = קבוע | `1` = מס' קופה | `a` = משמרת
* **קובץ PS (מוצרים):** מכיל את עסקאות החנות/מוצרים (שאינם דלק).
  * *מבנה שם הקובץ:* `ps060418.d1a` (אותו פורמט מבנה).

---

## 5. 🔍 פענוח לוגים וטיפול בהפרשים בדוחות

### 🔤 מקרא שורות בלוג ה-`dlg`
| שורה / קוד | משמעות פונקציונלית |
| :--- | :--- |
| `start process` | התחלת עסקה חדשה |
| `track 2` | העברת כרטיס פס מגנטי |
| `op:sp` | תעריפי הדלק בתחנה (מופיע אחרי הנקודתיים) |
| `op:ct` | תקרת תדלוק (תקרה בשקלים ולאחר מכן בליטרים) |
| `handle 1/2/3` | הרמת ידית במשאבה (לפי מספר הפיה) |
| `handle 0` | החזרת ידית המשאבה למקום |
| `op:nz` | התאפסות משאבות בתחילת תדלוק |
| `op:fs` | זרימת דלק בזמן אמת (סכום בש"ח ➔ ליטרים) |
| `op:ft` | שורת סיכום עסקה תקינה |
| `rs rec` | השורה שנשלחה ונרשמה בקובץ ה-RS |
| `int in` | שליחת בקשת אישור עסקה לשב"א |
| `int ot` | תשובת שב"א (`00` = אושר, קוד אחר = שגיאה/חסום) |
| `vsu event` | פתיחת עסקת תדלוק בהתקן דלקן |
| `v0` | ניתוק התקן דלקן |
| `dor box car no` | מספר רכב של ההתקן |
| `dor card` | מספר זיהוי ייחודי של ההתקן |
| `dor-ch in/ot` | בקשה ותשובה משרת דור אלון |
| `op:sl` | בוצעה הגבלת תדלוק |

* **פורמט שם קובץ לוג:** `dlg1907.0301` (`1907` = תאריך 19/07, `03` = מס' משאבה, `01` = מס' סידורי).

---

### 🛠️ נוהל תיקון הפרשי מונים (פער בעמודת כסף בלבד)

כאשר מופיע הפרש באדום בעמודת הכסף (ללא פער בליטרים), מדובר בחסר במונה פתיחה או סגירה.

```text
[זיהוי המשאבה והמשמרת] ➔ [העתקת קובץ RS לתיקייה מקומית] ➔ [גיבוי הקובץ ב-old] ➔ [השלמת שורות מונים מ-RS סמוך] ➔ [עדכון פרמטרים] ➔ [החזרה לתחנה והפקת TS]
```

1. **איתור:** מבררים מול הלקוח באיזו משמרת/משאבה מופיע ההפרש ומזהים אם חסר מונה פתיחה או סגירה.
2. **משיכת קובץ:** נכנסים לנתיב `C:/dda/station/data`, מעתיקים את קובץ ה-RS הרלוונטי למחשב התומך (תחת תיקיית `rss/נתיב_עבודה`).
3. **גיבוי חובה:** יוצרים תיקייה בשם `old` ומעתיקים אליה עותק נקי של קובץ ה-RS.
4. **השלמת נתונים:**
   * אם חסר **מונה סגירה** (למשל במשמרת בוקר): מעתיקים את שורות הפתיחה של המשאבות הרלוונטיות מקובץ ה-RS של **המשמרת הבאה**.
   * אם חסר **מונה פתיחה**: מעתיקים את שורות הסגירה מקובץ ה-RS של **המשמרת הקודמת**.
5. **עריכה:** מדביקים את השורות בסוף קובץ ה-RS (מתחת לשורות הסגירה).
   * מעדכנים תאריך ושעה נכונים.
   * קובעים ערכים: עבור מונה סגירה שמים **`9`-ים**, עבור מונה פתיחה שמים **`0`-ים**.
6. **סיום:** מחזירים את הקובץ המתוקן לתיקיית `data` בתחנה ופותחים מחדש את הדו"ח.
7. **בתחנות דור אלון בלבד:** קליק ימני על המשמרת ➔ `צור קובץ TS`.

---

### 📉 טיפול בהפרשים במינוס

#### 1. עסקה שנקטעה עקב ריסט (אין שורת `FT`)
* **תופעה:** פער קטן בליטרים בדוח.
* **שלבי אבחון:**
  1. משיכת קבצי RS לתאריך המבוקש וגיבוי ב-`old`.
  2. איתור המשאבה והפיה ע"י הפעלת המודול `pump_ck`:
     * הרצת `pump_ck` על קובץ ה-RS (לחיצה על `Ctrl + Enter` על `pump_ck` ➔ בחירת קובץ ה-RS ➔ `Enter`).
     * נוצר קובץ `comp.asc`. פותחים ב-**F4** וגוללים לסוף הקובץ.
     * בודקים בעמודת `diff` (הפרש במינוס יופיע ב-`comp.asc` בסימן `+`). העמודות השמאליות מציגות מס' משאבה ומס' פיה.
  3. משיכת לוגים: בתחנה ➔ `טכנאים` ➔ `filer` ➔ סינון לפי משאבה ותאריך ➔ `transfer`. העתקת קבצי `dlg` מנתיב `C:/dda/station/4upload`.
  4. בחינת ה-`dlg`: חיפוש השעה הרלוונטית. נראה שורות `fs` (זרימה) שלאחריהן מופיע ריסט ללא שורת `ft`.
  5. **טיפול:** העתקת פרטי העסקה (מס' רכב, התקן/אשראי, מס' עסקה, תאריך ושעה). אם מדובר בהתקן/מזומן - מעבירים לתחנה. אם מדובר באשראי - בודקים חיוב בשב"א ומזכים/מחייבים בהתאם.

#### 2. עסקה שנרשמה בסכום 0
* **תופעה:** דוח מראה הפרש בליטרים והפרש קטן בשקלים.
* **אבחון:** ב-`comp.asc` מזהים את המשאבה. בקובץ ה-RS הכל נראה רציף (אין מונים כפולים).
* **טיפול:** מסננים את קובץ ה-RS (מומלץ ב-Excel) ומאתרים עסקה שנסגרה בסכום `0`. מוציאים את שעת העסקה, מודקים בלוג את הנתונים ופועלים לחיוב/עדכון.

#### 3. עסקה שהתאפסה במהלך תדלוק
* **תופעה:** זרימת דלק החלה מחדש באמצע תדלוק והלקוח חויב רק על החלק השני.
* **אבחון:** ב-RS רואים זרימת `fs`-ים תקינה, ופתאום באמצע (ללא שורת `ft`) מתחילה זרימת `fs`-ים מ-`0`. העסקה נסגרה רק על הסכום שאחרי האיפוס.
* **טיפול:** מחייבים את הלקוח על ההפרש (סכום החלק הראשון שנלקח מ-`comp.asc`).

---

### 📈 טיפול בהפרשים בפלוס

#### 1. עסקה חויבה ללא יציאת דלק
* **אבחון:** הפעלת `pump_ck` מייצרת קובץ `comp.asc` שבו ההפרש מופיע בסימן `(-)` (כלומר עודף בתחנה).
* **בלוג:** מזהים שורת `ft` (סיכום תדלוק) שפניה **לא מופיעות** שורות `fs` (זרימת דלק).
* **סיבה:** חיוב יתר מזיכרון המשאבה או מתדלוק קודם.

#### 2. עסקה חויבה פעמיים עקב ריסט
* **אבחון:** ב-RS מזהים עסקת כפולה בסכום ההפרש (אחת בסוף משמרת קודמת ואחת בתחילת משמרת נוכחית).
* **בלוג:** עסקה ראשונה תקינה עם `fs`-ים ו-`ft`. עסקה שנייה מופיעה ללא `fs`-ים ועם סכום זהה ב-`ft`.
* **טיפול:** זיכוי לקוח באשראי או העברת פרטים לתחנה.

---

### 🔄 הפרשים מיוחדים (ללא פער כספי בפועל)
* **הפרש מתקזז:** עסקה שנפתחה במשמרת אחת ונסגרה במשמרת עוקבת. באיחוד דוחות של 2 המשמרות ההפרש מתקזז ל-0 (`+` במשמרת אחת ו-`-` בשנייה).
* **הפרשי דלקים:** פער בסוגי הדלקים על אותו הסכום exact (`+` בסוג אחד, `-` בסוג שני). נובע מ"משחק ידיות" (הרמת פיה 1, החזרה, והרמת פיה 2).
* **טיפול:** מסבירים ללקוח שמדובר בהפרש טכני מתקזז ואין צורך/אפשרות לתקן בדוחות.

---

## 6. ⚙️ מערכת תצורה וקונפיגורציה

כניסה: `ניהול` ➔ `תצורה` ➔ הזנת סיסמה (`100` או `22`) ➔ `Enter`.

### 📋 קודי סוגי דלק ושירותים
| קוד | תיאור | קוד | תיאור |
| :---: | :--- | :---: | :--- |
| **0** | סולר | **8** | גז |
| **2** | בנזין 96 | **10** | שמן |
| **3** | נפט | **90** | שירות לילה |
| **5** | בנזין 95 | **91** | שירות שבת |
| **6** | אוריאה | **92** | שירות חג |
| **7** | בנזין 98 | **93** | שירות חג |

### 🏷️ קודי זיהוי מערכתיים
* `07` = כרטיס עובד / מתדלק
* `315` = שלט מסטר עובד
* `106` = כרטיס מת"ס
* `120` = התקן דלקן
* `4000` = ישראכרט

---

## 7. 💳 מערכת שב"א וסליקת אשראי

מערכת שב"א מרכזת את כל חיובי וזיכויי האשראי בתחנה.

### 📤 ביצוע שידור יזום
* **נתיב:** `שב"א` ➔ `שידור`. (מעביר את הכספים בפועל לחשבון התחנה).

### 🖨️ הדפסת דוח שידור אחרון
ניתן להפיק בשתי דרכים:
1. `שב"א` ➔ `הדפסת דוח אחרון`.
2. `שב"א` ➔ `אשראית` ➔ `דוחות מערכת` ➔ סימון הדוח הרצוי ➔ לחיצה על `דוחות תקשורת` ➔ `Yes` ➔ `הדפסה`.
*(הערה: במסך דוחות מערכת מוצגים 99 השידורים האחרונים).*

---

## 8. 📊 ניהול מלאי דלקים

נתיב: `שב"א` ➔ `ניהול מלאים`.

### 🚛 הוספת מלאי ממיכלית (מילוי עצמי)
1. בחירת המיכל המבוקש ➔ לחיצה כפולה (Double Click) שמאלית.
2. בחלון שנפתח: הקלדת **תעודת משלוח** + **כמות ליטרים להוספה**.
3. לחיצה על `אישור` ווידוא עדכון הכמות.

### 📝 עדכון מלאי נוכחי ידני
1. בחירת המיכל ➔ לחיצה כפולה שמאלית.
2. לחיצה על `עדכון מלאי`.
3. הזנת כמות המלאי הנוכחית בפועל ➔ `אישור`.

---

## 9. 🔧 חיוב / זיכוי אשראי יזום

מבוצע מהמחשב של התומך במקרה של צורך בחיוב/זיכוי ידני:

```text
[הפעלת shva_sheli_ash95] ➔ [שידור ראשוני] ➔ [הזנת עסקה] ➔ [העתקת קובץ tran לתחנה] ➔ [שידור כפול בתחנה] ➔ [תיעוד בפריוריטי]
```

1. **הפעלה:** במחשב התומך דרך Salamander, כניסה לנתיב `C:/shva_sheli_ash95.exe`.
2. **שידור ראשוני:** ביצוע שידור בתוכנה.
3. **הזנת עסקה:**
   * כניסה ל-`ביצוע עסקאות`.
   * בחירת סוג עסקה: `חובה` (חיוב) / `זכות` (זיכוי).
   * הזנת **מספר כרטיס אשראי** + **תוקף** (לפחות חודש קדימה) + **סכום העסקה**.
   * לחיצה על `ביצוע`.
4. **חילוץ קובץ:** נוצר קובץ בנתיב `C:/shva_sheli/tran`. מעתיקים אותו לתיקייה זמנית.
5. **אימות שידור:** מבצעים שוב שידור בתוכנית התומך ומוודאים שסכום השידור זהה לסכום העסקה.
6. **העברה לתחנה:**
   * מוודאים שאין תדלוקי אשראי פעילים כרגע בתחנה.
   * מעתיקים את הקובץ `tran` מהמחשב שלנו אל התחנה לנתיב: `C:/dda/station/shva`.
7. **קליטה בתחנה:** עוברים למחשב התחנה ומבצעים **שידור פעמיים**.
8. **אימות ורישום:** מוודאים שהעסקה נקלטה בתוכנת `אשראית`, ורושמים את **מספר השידור** ו-**האסמכתא** בפריוריטי ובדף התיעוד.

---

## 10. 🚨 תקלות נפוצות ופתרונן

### 💳 1. כרטיס מתדלק / עובד לא מתקבל
* **תסמין:** הכרטיס לא עובר במשאבה ולא פותח תדלוק.
* **שלבי בדיקה:**
  * **כרטיס חדש:**
    1. בדיקת מגנוט: מבקשים מהעובד להעביר כרטיס. נכנסים ב-Salamander ל-`C:/dda/station/log/dlg...` במשאבה הרלוונטית. מחפשים קריאת כרטיס המתחילה ב-`07`.
    2. בדיקת הגדרה: בודקים אם מוגדר בקובץ `C:/dda/station/ch.prm` (בשורת `track 2`) או בניהול ➔ `תצורה` ➔ `עובדים`.
    3. *חובה לבצע ריסט למערכת לאחר כל שינוי הגדרה!*
  * **כרטיס שעבד בעבר:**
    1. **פס מגנטי שחוק:** אם בהעברה בלוג לא נרשמת אף שורה - הפס שחוק ויש להחליף כרטיס.
    2. **הגדרה שנמחקה:** מגדירים מחדש בתצורה / `ch.prm`.

---

### 📻 2. שלט מסטר לא מגיב
* **תסמין:** שלט אלחוטי לא פותח תדלוק במשאבה.
* **פתרון:**
  1. מבקשים ללחוץ על השלט במשאבה.
  2. נכנסים בלוג לשורת `op:ri` ומחלצים את קוד המגנוט (מתחיל ב-`315`).
  3. מוודאים שהקוד מוגדר ב-`ch.prm` ובמסך `תצורה` ➔ `מאסטר`.

---

### 🏷️ 3. כרטיס הנחה לא מתקבל (Display Message [107])
* **תסמין:** מופיעה שגיאה 107 ("לא ניתן לתדלק בכרטיס זה").
* **פתרון:**
  1. העברת הכרטיס במשאבה וחילוץ המגנוט בשורת `card` בלוג (מבנה: `0803460101` - צהוב: מגנוט קבוע, ירוק: סידורי, אדום: סוג הנחה).
  2. הגדרת הכרטיס ב-`ch.prm`.
  3. הגדרת גובה ההנחה וסוג הדלק בקובץ `discount.dat` (בנתיב `C:/dda/station/prices`) או דרך `תצורה` ➔ `הנחות`.

---

### 💰 4. עדכון מחירון בתחנה
* **דרך המשתמש:** `ניהול` ➔ `תצורה` ➔ הזנת תאריך ב-`מחיר עתידי` ➔ עדכון מחירים ➔ שמירה וסגירת משמרת / ריסט.
* **דרך התומך:** עריכת הקובץ `C:/dda/station/prices/prices.dat` ➔ הגדרת תאריך עתידי בשורת `inuse` ➔ עדכון מחירים ללא רווחים מיותרים ➔ שמירה וריסט.

---

### 🧾 5. עריכת כותרת/סיומת קבלה
* **דרך המשתמש:** `ניהול` ➔ `תצורה` ➔ `קבלה` ➔ עריכת טקסט ראשון/סופי ➔ שמירה וריסט (שים לב לסוג המשאבה בצד ימין).
* **דרך התומך:** עריכה ישירה של הקבצים `recipt.hdr` או `recipt.pos`.

---

## 11. 🖥️ מסלקות (DDA / DDM / POS)

### 🔌 סוגי תצורות תקשורת
| מסלקה | סוג תקשורת | הגדרה / נתיב |
| :--- | :--- | :--- |
| **DDA2** | תקשורת IP | `טכנאים` ➔ `dda2` ➔ `Setting` ➔ `Device` ➔ `Add` ➔ הזנת `DDA AD IP` ו-`Terminal 1/2 IP` |
| **DDA Light** | תקשורת IP | הגדרה על בסיס כתובות IP |
| **DDM** | חיבור סריאלי COM | מציג יציאות COM במסך הממשק |
| **DDA1** | חיבור סריאלי COM | מציג יציאות COM |

* **הגדרת נקודה עשרונית:** בלשונית `Decimal Point`.
* **מסלקת DDA:** מציגה את כל המשאבות, הרמות ידית ותדלוקים פעילים.
* **מסלקת POS:** מציגה את הטרמינלים בממשק המשאבה ופעולות לקוח (הקשת ת.ז., מס' רכב, בחירת דלק).

---

## 12. 🔄 תרשים טיפול בפוס תקוע

נתיב ניווט: `טכנאים` ➔ `POS`.

```text
                       [התקבל דיווח: POS תקוע]
                                  │
                  בדיקת מסלקת POS: האם מופיע ריבוע אדום?
                                  │
                 ┌────────────────┴────────────────┐
                 ▼                                 ▼
            [אין ריבוע אדום]                   [יש ריבוע אדום]
                 │                                 │
   בדיקת שגיאה ב-errors.pos               תחקור הלקוח:
   וניסיון ריסט למערכת                     1. מה כתוב על המסך?
                 │                         2. ניסיון פתיחת תדלוק
        ┌────────┴────────┐                3. האם לחצנים מגיבים?
        ▼                 ▼                        │
     [עזר]             [לא עזר]                    │
        │                 │                  ביצוע צריבת פוס /
  סגירת תקלה       1. הורדת מערכת           הורדת חשמל לפוס
                   2. מחיקת backprint מ-c/dda/station
                   3. ריסט חשמלי לפוס              │
                   4. העלאת חשמל ומערכת    ┌───────┴───────┐
                                           ▼               ▼
                          │             [עזר]           [לא עזר]
                   ┌──────┴──────┐         │               │
                   ▼             ▼     סגירת תקלה   פתיחת תקלה לטכנאי
                [עזר]         [לא עזר]                 (רוזמן)
                   │             │                         │
             סגירת תקלה     ריסט למערכת                    ▼
                                 │                   העברת קריאה לגלית
                                 ▼                         │
                          פנייה למנהל/ת                    ▼
                          מוקד תמיכה                 פנייה למנהל/ת
                                                     מוקד תמיכה
```

---
*נערך ועוצב עבור צוות תמיכה טכנית - זמין לשימוש וניווט מהיר ב-GitHub.*
