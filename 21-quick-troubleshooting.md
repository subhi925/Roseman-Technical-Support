# 21 – טבלת תקלות מהירה – אבחון ראשון

## כרטיס עובד לא מתקבל

חדש? → בדיקת track 2 → CH.PRM/Workers. היה עובד? → בדיקת מגנוט פיזי והגדרה.

## שלט לא מגיב

בדיקת מגנוט → CH.PRM → Master/תצורה.

## כרטיס הנחה לא נותן הנחה

בדיקת card → הגדרת כרטיס → discount.dat → תצורה/סוג דלק.

## דור 3 לא עובד

RFU → זיהוי → סוללה → אחריות → לוג → VSU2RFC.

## דור 4 לא עובד

CVS → SAM1 → COMDA/CCID → RFU/CA → IP/VSU2RFC → לוג.

## כל ההתקנים לא עובדים

תחילה בידוד רכיב משותף: CVS/Java/COMDA/CA/RFU.

## מסך מלאים ריק

בדיקת data.sav/data.ini וגודל קבצים; לאחר החלפה נדרשת מדידה לפי המקור.

## Recovery תקוע

בדיקת recovery → העברה ל־temp → Reset → אימות.

## הפרש בדוח

קודם לסווג: מונה, Reset, סכום 0, איפוס תדלוק, חיוב ללא דלק, חיוב כפול או הפרש מתקזז.

## Nayax – יחידה לא משויכת

Unit must exist first → שיוך לתחנה → שיוך למשאבה.

## מועדון דור אלון

בדיקת התאמה EMV → pos.prm/dda.prm → לוג ClubDor2026 → שרת/פורמט מספר.

---

[⬆ חזרה לראש](../README.md)
