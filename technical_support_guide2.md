# ⛽ מדריך ניווט ותמיכה טכנית - מערכות תחנות דלק (Fuel Station Technical Support Guide)

[![GitHub - Documentation](https://img.shields.io/badge/Documentation-Fuel_Station_Support-blue.svg)](#)
[![Version](https://img.shields.io/badge/Version-1.0.0-green.svg)](#)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](#)

מסמך זה משמש כמדריך ניווט מהיר ואבחון תקלות עבור טכנאי תמיכה טכנית במערכות אוטומציה, ניהול משאבות, מסופי אשראי והתקני תדלוק בתחנות דלק.

---

## 📌 תוכן עניינים (Table of Contents)

1. [🚨 תקלות שכיחות ופתרונות מהירים](#1--תקלות-שכיחות-ופתרונות-מהירים)
2. [📁 קבצי תצורה מרכזיים ונתיבים במערכת](#2--קבצי-תצורה-מרכזיים-ונתיבים-במערכת)
3. [📡 אבחון מערכות זיהוי התקנים (דור 3 ודור 4)](#3--אבחון-מערכות-זיהוי-התקנים-דור-3-ודור-4)
   * [דור 3 (Gen 3)](#דור-3-gen-3)
   * [דור 4 (Gen 4) - אינדיקציית לדים](#דור-4-gen-4---אינדיקציית-לדים)
   * [דור 4 - ממשק CVS (Web Interface)](#דור-4---ממשק-cvs-web-interface)
4. [🏪 מאפיינים ודגשים לפי רשתות דלק](#4--מאפיינים-ודגשים-לפי-רשתות-דלק)
   * [רשת טן (Ten)](#רשת-טן-ten)
   * [רשת דור אלון](#רשת-דור-אלון)
   * [רשת פז (Paz / Pazomat)](#רשת-פז-paz--pazomat)
5. [🛠️ פקודות שימושיות ונתיבים מהירים](#5--פקודות-שימושיות-ונתיבים-מהירים)

---

## 1. 🚨 תקלות שכיחות ופתרונות מהירים

| תסמין / תקלה | סיבה משוערת | פתרון מומלץ |
| :--- | :--- | :--- |
| **מסך ריק בניהול מלאים** | קבצים `data.sav` ו-`data.ini` בנתיב `c:/dda/station/cim` בגודל 0 bytes. | העבר את הקבצים מכונן הגיבוי `D:` או מתחנה עובדת. לאחר מכן, בצע מדידה יזומה ע"י מנהל התחנה. |
| **ג'יבריש / אנגלית במסך התצורה** | קובץ התרגום `config_dict.d` בתיקייה `config` פגום או חסר. | העתק את הקובץ מתחנה תקינה אחרת או מכונן הגיבוי `D:`. |
| **משאבה תקועה ב-Recovery** *(חץ ירוק וורוד)* | קיימים קבצים שלא עלו בסיום עסקה במערכת. | העבר את הקבצים מתיקיית `recovery` ל-`c:/temp` (באמצעות מקש **F6**) ובצע איפוס (Reset) למערכת. |
| **קובץ חסומים לא מעודכן (שב"א)** | תקשורת שב"א מעוכבת או קובץ חסומים מקומי תקוע. | 1. הרץ שידור יזום באמצעות `transmit_now.bat` בתיקיית `Shva`.<br>2. אם לא עוזר: שנה סיומת של הקבצים `neg` ו-`jenr` בתיקיות `shva` ו-`cr_tran` ובצע שידור מחדש. |
| **חלון שגיאה קופץ של `g3analyzer`** | תהליך ניתוח נתונים תקוע או קובץ גיבוי פגום. | 1. סגור את התהליך דרך ה-Task Manager.<br>2. אם התקלה חוזרת, מחק/העבר קבצים בתיקייה `c:/dda/station/vsu/dysave/backup` והעלֵה את המערכת מחדש. |
| **זיהוי ב-CVS אך אין זיהוי ב-DLG** | חוסר התאמה בכתובות IP פנימיות. | הגדר את כתובת ה-IP ב-CVS ובקובץ `vsu2rfc.ini` ל-`127.0.0.1` ובצע ריסט למערכת. |
| **סטטוס Sam1 כבוי / התקני דור 4 לא מתדלקים** | רכיב Java תקוע או בעיית זיהוי חומרה (Comda). | 1. בצע אתחול ל-Java (דרך CVS: פעולות -> אתחול CVS, או סגירת Java ב-Task Manager).<br>2. ודא מול מנהל התחנה שרכיב ה-Comda מחובר ומזוהה ב-Device Manager תחת `Smart card readers` כ-`CCID`. |
| **אנטנות באדום ב-CVS** | אובדן תקשורת מול היחידה המרכזית (CA). | 1. שלח פקודת `ping` לכתובת ה-CA.<br>2. אם אין מענה: בדוק כבלי רשת / בצע איפוס חשמלי ל-CA.<br>3. אם יש מענה: כנס ל-`progca` וודא שסומן V בפרמטר `rfu-universal`. |

---

## 2. 📁 קבצי תצורה מרכזיים ונתיבים במערכת

> [!IMPORTANT]
> לפני ביצוע שינויים בקבצי PRM או POS, מומלץ ליצור עותק גיבוי (`.bak`) של הקובץ המקורי.

### ⚙️ מפת קבצים ראשית

```text
c:/dda/station/
├── station.prm       # הגדרות משאבות, פיות, כתובות תקשורת, סוגי דלק ושירות
├── station.pos       # הגדרת שיוך משאבות לטרמינלים ופוסים בחנות/עמדה
├── dda.prm           # פרמטרים כלליים: מחירונים, מע"מ, גבולות תדלוק, פורט COM
├── pos.prm           # הגדרות טרמינלים, מדפסות קבלה ושיוך משאבות ללא טרמינל
├── Ch.prm            # הגדרת כרטיסי עובד/מתדלק, כרטיסי הנחה ושלטי מחיר
└── dor/
    └── w.list        # רשימת לקוחות מורשים (Whitelist) לתדלוק
```

<details>
<summary><b>🔍 פירוט נוסף על קבצי התצורה (לחץ להרחבה)</b></summary>

#### `station.prm`
* מגדיר את מיפוי המשאבות והפיות בתחנה.
* מכיל כתובות תקשורת, משייך סוג דלק, קופה שולטת, והבחנה בין שירות מלא לשירות עצמי.
* **להורדת משאבה מתפקדת מהמערכת:** יש להוסיף `//` בתחילת השורה המתאימה, לשמור את הקובץ ולבצע ריסט למערכת.

#### `dda.prm`
* מגדיר את כלל הפרמטרים האפליקטיביים של התחנה.
* כולל טבלאות מחירונים, שיעור מע"מ עדכני, גבולות תדלוק מותרים (מינימום/מקסימום לליטר/סכום).
* מגדיר את יציאת ה-COM המשמשת לתקשורת משאבות בגרסאות DDA1.

#### `pos.prm` & `station.pos`
* **`pos.prm`**: מגדיר את סוגי הטרמינלים והפוסים, הגדרות הדפסת קבלה (`Ask print`), ושיוך משאבות שאין להן טרמינל פיזי להדפסה ישירה בפוס.
* **`station.pos`**: מגדיר את החפיפה בין עמדות המכירה (POS) למשאבות בשטח.

#### `Ch.prm`
* קובץ ניהול הרשאות ושלטים.
* הגדרת כרטיסי עובד (`Worker`), כרטיסי הנחה (`Discount`), ושלטים פעילים בתחנה (`MasterId`).

</details>

---

## 3. 📡 אבחון מערכות זיהוי התקנים (דור 3 ודור 4)

### דור 3 (Gen 3)
* **אופן זיהוי:** זיהוי התקנים מתבצע באמצעות קוד רכב/התקן בן **6 ספרות**.
* **בדיקת מתח סוללה:**
  ```text
  rfu -> option -> view reader bat
  ```
> [!NOTE]
> * **מתח תקין:** עד **3.6V**.
> * **מתח לא תקין:** מתח הנמוך מ-**2.8V** מעיד על סוללה פרוקה/קורא לא תקין – יש להחליף את היחידה.

---

### דור 4 (Gen 4) - אינדיקציית לדים

| מצב לדים בקורא | משמעות |
| :--- | :--- |
| 🔴🔴 **2 הבהובים אדומים מהירים** | קורא במצב שינה (Sleep Mode). |
| 🟢🟢 **2 הבהובים ירוקים מהירים** | זיהוי תקין של טבעת תדלוק. |
| 🔴 **הבהוב אדום רציף** | זיהוי התקן מסוג דור 3. |
| 🟢 **הבהוב ירוק רציף** | זיהוי התקן מסוג דור 4. |
| 🔴🟢 **אדום-ירוק מתחלף ללא הפסקה** | הקורא נמצא בתהליך צריבת גרסה / עדכון תוכנה. |

---

### דור 4 - ממשק CVS (Web Interface)

ניתן להתחבר לממשק הניהול בדפדפן בכתובת המקומית:
`http://127.0.0.1:8008`

> [!TIP]
> **בדיקת ערוצי אנטנות בממשק:**
> * `f-rfu` (קורא אקדח): פעיל בערוצים **2, 3, 5**.
> * `vehical-rfu/z` (אודומטר/ספידומטר): פעיל בערוץ **17**.

* **שחרור נעילה / חבלה (Tamper):**
  1. היכנס ללשונית **קוראי אקדח**.
  2. סמן `V` בתיבת הרישום **"בטל נעילה/חבלה"**.
  3. לחץ על **שמירה**.
* **מספר קורא בצבע סגול בממשק:** מציין שהקורא נכנס למצב נעילה (`Tamper`) כתוצאה מזיהוי ממושך או ניתוק רציף.

---

## 4. 🏪 מאפיינים ודגשים לפי רשתות דלק

### רשת טן (Ten)
* **גישה לתפריט חברה בפוס:**
  `מקש מיוחד` ⬅️ `כרטיס מתדלק` ⬅️ `מקש 6`
* **פעולות זמינות בתפריט:**
  * כיול משאבות ופיצוי לקוחות.
  * רישום צריכה עצמית.
  * הפקדת מזומן (*מצריך הגדרת `mode=3` בקובץ `station.pos`*).
  * ביטול עסקה (מוצרים בלבד).
  * המרת עסקה (`st=3`) ועסקה משולבת (`st=5`).
* **הגדרת סורק ברקוד:** מבוצעת בקובץ `pos.prm` תחת הפרמטר `barcode-ip`.

---

### רשת דור אלון
* **שיטת תמחור:** עבודה במודל של 3 רמות מחיר:
  1. מחיר מזומן
  2. מחיר אשראי
  3. מחיר גבוה (מוגדר בקובץ `clubdor/discmdn.dat`)
* **מועדון You:** כרטיסי מועדון You מעניקים הנחה נגזרת מתוך המחיר הגבוה.

---

### רשת פז (Paz / Pazomat)
* **נתיב ניהול פזומט:** `c:/dda/station/pazomat`
* **שידור יזום לפזומט:** הרצת הסקריפט `Pazcall.cmd`.
* **אבחון שגיאות בלוג (`lc`):**
  * `Error 40` בלוג מציין כי כלי הרכב המבוקש אינו מופיע ברשימה הלבנה (Whitelist / רכב לא מורשה).

---

## 5. 🛠️ פקודות שימושיות ונתיבים מהירים

```cmd
:: הרצת שידור יזום לשב"א
cd C:\dda\station\shva
transmit_now.bat

:: הרצת שידור פזומט (רשת פז)
cd C:\dda\station\pazomat
Pazcall.cmd

:: בדיקת תקשורת ל-CA
ping 192.168.1.100

:: נתיב גיבוי דיאגנוסטיקת g3analyzer
C:\dda\station\vsu\dysave\backup
```

---

<div align="center">
  <sub>נערך עבור צוות תמיכה טכנית | נבדק ומותאם להעלאה ל-GitHub</sub>
</div>
