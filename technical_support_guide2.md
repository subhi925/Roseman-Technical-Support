# ⛽ מדריך ניווט ותמיכה טכנית - מערכות תחנות דלק (Roseman / DDA Systems)

![Support Level](https://img.shields.io/badge/Support-Tier_1_%26_Tier_2-blue)
![System](https://img.shields.io/badge/System-DDA_%7C_POS_%7C_CVS_Gen4-green)
![OS Compatibility](https://img.shields.io/badge/OS-Windows_XP_%7C_7_%7C_10-orange)

מדריך ניווט מקיף, אבחון תקלות, נתיבי מערכת והנחיות עבודה עבור טכנאי תמיכה במערכות אוטומציה, ניהול משאבות, מסופי אשראי והתקני תדלוק בתחנות דלק.

---

## 📌 תוכן עניינים (Table of Contents)
1. [🚨 אבחון ופתרון תקלות מהיר](#-אבחון-ופתרון-תקלות-מהיר)
2. [📁 מפת קבצי תצורה, נתיבים ופרמטרים](#-מפת-קבצי-תצורה-נתיבים-ופרמטרים)
3. [📡 מערכות זיהוי התקנים (דור 3 ודור 4)](#-מערכות-זיהוי-התקנים-דור-3-ודור-4)
4. [🔥 מדריך צריבות ועדכוני תוכנה](#-מדריך-צריבות-ועדכוני-תוכנה)
5. [🏪 מאפיינים ודגשים לפי רשתות דלק](#-מאפיינים-ודגשים-לפי-רשתות-דלק)
6. [🛠️ פקודות שימושיות וסקריפטים להרצה](#-פקודות-שימושיות-וסקריפטים-להרצה)

---

## 🚨 אבחון ופתרון תקלות מהיר

| תסמין / תקלה | סיבה משוערת | פתרון מומלץ | נתיב / קובץ רלוונטי |
| :--- | :--- | :--- | :--- |
| **מסך ניהול מלאים ריק** | הקבצים `data.sav` ו-`data.ini` בגודל 0 bytes. | העבר קבצים תקינים מכונן `D:` או מתחנה עובדת. לאחר מכן יש לבצע מדידה יזומה. | `c:/dda/station/cim` |
| **ג'יבריש / אנגלית בתצורה** | קובץ התרגום `config_dict.d` פגום או חסר. | העתק את הקובץ מתחנה תקינה אחרת או מכונן `D:`. | `c:/dda/station/config` |
| **משאבה תקועה ב-Recovery (חץ ירוק וורוד)** | קיימים קבצי עסקאות שלא עלו בסיום תדלוק. | העבר את הקבצים מתיקיית `recovery` ל-`c:/temp` (באמצעות F6) ובצע ריסט למערכת. | `c:/dda/station/recovery` |
| **שגיאה "קובץ חסומים לא מעודכן" (שב"א)** | תקשורת שב"א מעוכבת או קובץ חסומים מקומי תקוע. | 1. הרץ `transmit_now.bat`.<br>2. שנה סיומת לקבצים `neg` ו-`jenr` בתיקיות `shva` ו-`cr_tran` והרץ שידור מחדש. | `c:/dda/station/shva` |
| **שגיאה קופצת g3analyzer** | תהליך ניתוח נתונים תקוע או קובץ גיבוי פגום. | 1. סגור את התהליך ב-Task Manager.<br>2. מחק/העבר קבצים בתיקייה `dysave/backup` והעלֵה מחדש. | `c:/dda/station/vsu/dysave/backup` |
| **סטטוס Sam1 כבוי / דור 4 לא מתדלק** | Java תקוע או שרכיב ה-Comda (USB) אינו מזוהה. | 1. בצע אתחול ל-Java (דרך CVS: פעולות -> אתחול, או לסגור ב-Task Manager).<br>2. ודא זיהוי ה-Comda ב-Device Manager תחת Smart card readers כ-CCID. | `c:/dda/station/cvs` |
| **אנטנות באדום ב-CVS** | אובדן תקשורת מול יחידת ה-CA. | 1. שלח פקודת `ping` ל-CA.<br>2. אם אין פינג: בדוק כבלים / הורד חשמל ל-CA.<br>3. אם יש פינג: כנס ל-`progca` וודא שסומן V בפרמטר `rfu-universal`. | `c:/dda/station/vsu` |
| **זיהוי ב-CVS אך אין זיהוי ב-DLG** | אי-התאמה בכתובות IP פנימיות. | הגדר את כתובת ה-IP ב-CVS ובקובץ `vsu2rfc.ini` ל-`127.0.0.1` ובצע ריסט למערכת. | `c:/dda/station/vsu/vsu2rfc.ini` |
| **שגיאת Bad Communication ב-RFU (דור 3)** | נפילת תקשורת מול ה-CA. | נסה לבצע אתחול חשמלי (הורדת חשמל) ל-CA. אם לא עוזר, התייעץ עם טכנאי. | `c:/dda/station/vsu` |
| **RFU בג'יבריש (דור 3)** | תקלה בטעינת האפליקציה ב-RFU. | בלשונית RFU לחץ `get ver`. אם לא עוזר: לחץ `godo boot` ואז `godo appl` ולחץ שוב `get ver`. | תוכנת RFU |

---

## 📁 מפת קבצי תצורה, נתיבים ופרמטרים

### 🗂️ מבנה תיקייה ראשי: `c:/dda/station/`

```text
c:/dda/station/
├── station.prm       # הגדרות משאבות, פיות, כתובות תקשורת, סוגי דלק ושירות
├── station.pos       # הגדרת שיוך משאבות לטרמינלים ופוסים בחנות/עמדה
├── dda.prm           # פרמטרים כלליים: מחירונים, מע"מ, גבולות תדלוק, פורטי COM
├── pos.prm           # הגדרות טרמינלים, מדפסות קבלה, barcode-ip ושיוך משאבות
├── Ch.prm            # הגדרת כרטיסי עובד/מתדלק, כרטיסי הנחה ושלטי מחיר
├── rfc_3000.ini       # הפעלת מסלקות פעילות במערכת
├── rfc_proc.ini       # הגדרת תהליכים (Processes) מורשים בתחנה
├── rfc_menu.ini       # הגדרות תפריטים (ניהול מלאים, טכנאים)
├── Errors.log / .pos # קבצי תיעוד שגיאות מערכת ומסלקות
├── cim/              # ניהול מלאי מיכלים ידני (cim.ini, data.sav, data.ini)
├── config/           # קבצי תצורה ותרגום (config.exe, config_dict.d)
├── filesync/         # סקריפט גיבוי מכונן C: לכונן D: (filesync.xml)
├── prices/           # מחירונים (prices.dat, discount.dat, prodlist)
├── printman/         # הגדרות הדפסה וקבלות (printman.ini, gnrch.exe)
├── receipt/          # ארכיון קבלות בתחנה (Tran.XYYYYYY)
├── recovery/         # קבצי עסקאות פעילות/תקועות
├── report/           # הגדרות וסקריפטים של דוחות (newgen.exe, newgen.prm)
├── shva/             # רכיב אשראית ותקשורת שב"א (ash95.exe, transmit_now.bat)
├── dor/              # רשימות מורשים (w.list, whitelist.dat) ורכיבי תקשורת
├── cvs/              # מסלקת דור 4 (runcvs.bat, cvs.dbd, logs)
├── pazomat/          # מסלקת פזומט (pazomat_intr.exe, Pazcall.cmd)
└── roseman utils/    # כלי צריבה וגרסאות (burning tools, burning versions)
```

### ⚙️ פירוט קבצים ופרמטרים קריטיים

<details>
<summary><b>📄 station.prm (לחץ להרחבה)</b></summary>

* **תפקיד:** הגדרת מיפוי המשאבות והפיות בתחנה.
* **פרמטרים מרכזיים:**
  * `Pump`: מספר משאבה.
  * `Op-mode`: קיום טרמינל במשאבה (`2` = כן, `1` = לא).
  * `Isle` / `Position`: מיקום המשאבה במסלקת DDA (שורה / עמודה).
  * `Nozzle`: מספר הפיות במשאבה.
  * `Fuel type`: סוג הדלק המשוייך לפיה.
  * `Dda-id`: כתובת תקשורת (Address) של המשאבה.
  * `Cash`: שיוך לקופה.
  * `Spcl`: הגדרת סוג שירות (`1`/`0` = שירות עצמי, `2` = שירות מלא עם גביית תעריף לילה, `66` = שירות עצמי עם גביית תעריף לילה).
  * `Tank`: שיוך מיכל דלק בניהול מלאים.
* **הורדת משאבה מהמערכת:** יש לרשום `//` בתחילת השורה המתאימה בקובץ, לשמור ולבצע ריסט.
</details>

<details>
<summary><b>📄 station.pos & pos.prm (לחץ להרחבה)</b></summary>

* **`station.pos`:** מקשר בין משאבות עם טרמינל לבין עמדות המכירה (POS).
  * `pos`: מס' משאבה | `dock`/`loc`: שורה/עמודה | `addr`: כתובת טרמינל | `mode`: מוגדר כ-`1` (בטֵן `3` להפקדות מזומן).
* **`pos.prm`:** הגדרות טרמינלים, מדפסות ושיוך משאבות.
  * `Ask print`:
    * `0` = ללא שאלת הדפסה בסיום עסקה.
    * `1` = הדפסת קבלת מקור אוטומטית (ללא שאלת העתק).
    * `3` = הצגת שאלות "האם להדפיס מקור?" ו-"האם להדפיס העתק?".
  * `barcode-ip`: הגדרת 3 המקטעים הראשונים של IP הסורק (ברשת טן).
  * `print->pump7pos=20`: שיוך משאבה 7 ללא טרמינל להדפסה ישירה בפוס 20.
</details>

<details>
<summary><b>📄 dda.prm (לחץ להרחבה)</b></summary>

* **תפקיד:** הגדרת פרמטרים כלליים, מחירונים, מע"מ ופתרונות תקשורת DDA1.
* **איתור פורט COM עבור משאבת DDA1:**
  1. בדוק ב-`station.prm` מהו ה-`Dda-id` (הכתובת) של המשאבה.
  2. כנס ב-`dda.prm` לחלק התחתון תחת `[dda]`, חפש את ה-`add` ומצא תחת איזה `dda(x)` הוא יושב.
  3. עלה למעלה ומצא תחת איזה `port(y)` מוגדר ה-`dda(x)`.
  4. בדוק תחת `[Ports]` לאיזה `COM` מחובר אותו `port(y)`.
* **פרמטרים נוספים:**
  * `vat`: שיעור מע"מ.
  * `Max-fuel-limit` / `Max-money-limit`: גבולות כמות/סכום מותרים לתדלוק בודד.
  * `dor-price`: הגדרה ל-`yes` בתחנות דור אלון (עבודה עם 3 רמות מחיר).
</details>

<details>
<summary><b>📄 Ch.prm & W.list (לחץ להרחבה)</b></summary>

* **`Ch.prm`:** מנהל הרשאות ואמצעי תדלוק מורשים.
  * `Worker`: כרטיסי מתדלק/עובד (`07xxxxyy`).
  * `MasterId`: שלטים פעילים לתדלוק (`xxxyyy`).
  * `Discount`: כרטיסי הנחה (`08xxxxyyzz`).
* **`W.list` (בנתיב `dor/w.list` או `dor/whitelist.dat`):** הרשימה הלבנה של לקוחות וציים מורשים. מכיל מספרי רכב, מספרי התקנים וסוגי דלק מורשים.
</details>

---

## 📡 מערכות זיהוי התקנים (דור 3 ודור 4)

### 🔹 דור 3 (Gen 3)
* **זיהוי:** קוד התקן/רכב בן 6 ספרות.
* **בדיקת מתח סוללה בקורא אקדח:**
  1. כנס במסלקה: `טכנאים` -> `rfu` -> `option` -> `view reader bat`.
  2. **סף מתח:** מתח תקין הינו עד **3.6V**. מתח הנמוך מ-**2.8V** מעיד על סוללה גמורה/קורא תקול ויש להחליפו.
* **G3 Viewer / G3 Analyzer:**
  * בדיקת היסטוריה ואחריות קורא דרך `G3 Viewer` -> בלשונית `nozzle data` מכניסים מס' קורא. תוקף אחריות הינו **עד שנה וחצי** מיום ההתקנה.

### 🔹 דור 4 (Gen 4 - CVS)
* **ממשק ניהול:** התחברות בדפדפן בכתובת: `http://127.0.0.1:8008`
* **נוריות אינדיקציה (LEDs) בקורא האקדח:**

```text
🔴🔴 2 הבהובים אדומים מהירים  --> מצב שינה (Sleep Mode) לשמירת סוללה
🟢🟢 2 הבהובים ירוקים מהירים --> זיהוי תקין של טבעת התדלוק
🔴   הבהוב אדום רציף         --> זיהוי התקן מסוג דור 3
🟢   הבהוב ירוק רציף        --> זיהוי התקן מסוג דור 4
🔴🟢 אדום-ירוק מתחלף ברצף    --> הקורא בתהליך עדכון תוכנה / צריבה
```

* **ערוצי תקשורת RFU מומלצים:**
  * `f-rfu` (קוראי אקדח): ערוצים **2, 3, 5** (פורט 2600).
  * `vehical-rfu/z` (אודומטר/ספידומטר): ערוץ **17** למניעת חפיפה (פורט 2601).
* **טיפול במצב נעילה (Tamper):**
  * **תסמין:** קורא בצבע סגול בממשק CVS או דיווח ניתוק.
  * **שחרור:** כנס בלשונית קוראי אקדח, סמן **V** בתיבת `"בטל נעילה/חבלה"` ולחץ שמירה.
  * **פרמטר בדיקה:** בסט פרמטרים קוראים: ערך `3` = מתעלם מ-Tamper, ערך `7` = בודק Tamper.

---

## 🔥 מדריך צריבות ועדכוני תוכנה

### 1. צריבת DDA2 (משאבות חיבור IP)
* **תוכנה:** `c:/dda/station/roseman utils/burning tools/progdda2.exe` (גרסה 3.4)
* **שלבי ביצוע:**
  1. בלשונית `TCP`: הזן את כתובת ה-IP של המשאבה בשדה `Local IP`.
  2. ב-`Select connection`: סמן `TCP`.
  3. הסר והחזר את ה-V מ-`Local IP` ולחץ **Get** (הבהוב מאשר תקשורת תקינה).
  4. עבר ללשונית `DDA2` ולחץ **Get Ver** לצפייה בגרסה הנוכחית.
  5. לחץ `File` -> `Open` ועבור לנתיב: `roseman utils/burning versions/dda2`.
  6. בחר קובץ גרסה רצוי -> `Open` -> לחץ **Program**.

### 2. צריבת DDA / DDM (משאבות חיבור COM)
* **תוכנה:** `roseman utils/burning tools/callflash_1.7`
* **שלבי ביצוע:**
  1. הגדר פורט ב-`Set COM port` וקבע `Baud` ל-**28800** (אם נכשל, שנה ל-**9600**).
  2. תחת `DDA Add` -> `Current`: לחץ **Get** לקבלת כתובת, ואז **Get Ver**.
  3. לחץ **Open File**, נווט ל-`burning version/dda/dda`, בחר קובץ -> **Prog DDA**.

### 3. צריבת CA דור 3 ודור 4
* **CA דור 3:** תוכנת `vsu/progca2.7.exe` -> לחץ `Find CA` לקבלת IP -> `Get Ver` -> טען קובץ מתיקיית `ca versions` -> `Program`.
* **CA דור 4:** תוכנת `vsu/progca_03.exe` -> `Find CA` (זהה לפי IP תחנה בסיומת 66/67) -> טען קובץ `g4_cvs/ca_uni/rom` -> ודא שסומן **V** בפרמטר **`rfu-universal`** -> לחץ **Program**.

---

## 🏪 מאפיינים ודגשים לפי רשתות דלק

### 🟡 רשת טן (Ten)
* **תפריט חברה בפוס:** לחץ מקש `מיוחד` ⬅️ העבר `כרטיס מתדלק` ⬅️ הקש `6` ("אפשרויות חברה").
  * **פעולות זמינות:** כיול משאבות (קוד מעקב), פיצוי לקוחות, צריכה עצמית, ביטול עסקת מוצר.
  * **הפקדת מזומן:** דורש הגדרת `mode=3` בקובץ `station.pos` (נרשם ב-`ps` תחת קוד `000023`).
  * **עסקה משולבת:** תדלוק ומוצרים באותה עסקה (מסומן כ-`st=5` בקובץ `ps`).
  * **המרת עסקה:** המרת סוג תשלום (מסומן כ-`st=3` בקובץ `ps`).
* **סורק ברקוד:** מוגדר ב-`pos.prm` תחת הפרמטר `barcode-ip` (כתובת IP: מקטעי התחנה + `100` + מס' משאבה/פוס).

### 🔵 רשת דור אלון
* **מודל 3 מחירים:** מחיר מזומן, מחיר אשראי, ומחיר גבוה (מת"ס/ספידומט).
* **קובץ מחירון גבוה:** `c:/dda/station/clubdor/discmdn.dat`.
* **מועדון You:** העברת כרטיס You מעניקה הנחה נגזרת מהמחיר הגבוה.
* **שרת Prepaid:** מוגדר בקובץ `dclub_ch.ini` תחת `prepaidserverIp`.

### 🟢 רשת פז (Paz / Pazomat)
* **מסלקת פזומט:** מופעלת ע"י `pazomat_intr.exe` בתיקייה `c:/dda/station/pazomat`.
* **שידור יזום:** הרצת הקובץ `Pazcall.cmd`.
* **אבחון שגיאות בלוג (`lc` / `pi`):** שגיאה **Error 40** מציינת שהרכב המבוקש אינו מופיע ברשימה הלבנה המקומית (`locals.dat`).

---

## 🛠️ פקודות שימושיות וסקריפטים להרצה

```cmd
:: 1. הרצת שידור יזום לשב"א (שחרור קובץ חסומים)
cd C:\dda\station\shva
transmit_now.bat

:: 2. הרצת שידור יזום לפזומט (רשת פז)
cd C:\dda\station\pazomat
Pazcall.cmd

:: 3. בדיקת תקשורת פינג ליחידת מרכזית CA / סורק
ping 192.168.1.100 -t

:: 4. אתחול יזום למסלקת דור 4 (Java / CVS)
cd C:\dda\station\cvs
:: ניתן לבצע איפוס תהליך דרך Task Manager או להריץ מחדש את runcvs.bat

:: 5. נתיב קבצי גיבוי לפתרון שגיאת g3analyzer
cd C:\dda\stationsu\dysaveackup
```

---
*המדריך מעודכן ונבדק עבור מערכות רוזמן ומשאבות DDA.*
