# מדריך תמיכה טכנית - מערכות תחנה

מדריך זה מרכז את כלל המידע הטכני, הגדרות התצורה ופתרון התקלות עבור מערכות התחנה השונות, מיועד לשימוש מהיר בשטח.

## תוכן עניינים
- [1. מערכת FuelPoint וייצוא נתונים](#1-מערכת-fuelpoint-וייצוא-נתונים)
- [2. מכשירים ניידים והסבת קופה לפוס נייד (NOVA55F)](#2-מכשירים-ניידים-והסבת-קופה-לפוס-נייד-nova55f)
- [3. ניהול ותמיכה במערכת Nayax Core (DCS)](#3-ניהול-ותמיכה-במערכת-nayax-core-dcs)
- [4. תמיכה ושדרוג גרסאות (RFC ו-DDA2)](#4-תמיכה-ושדרוג-גרסאות-rfc-ו-dda2)
- [5. מועדון לקוחות דור אלון (מגרסה 6.2.20.45)](#5-מועדון-לקוחות-דור-אלון-מגרסה-622045)

---

## 1. מערכת FuelPoint וייצוא נתונים
מערכת זו משמשת לניהול משתמשים, מלאי והגדרות תחנה.

* **גישה למערכת:** ההתחברות מתבצעת דרך האינטרנט (אחריות הלקוח, חיבור IPvpn) או סלולר (אחריות רוזמן), ותמיכה טכנית מתבצעת לרוב באמצעות TeamViewer.
* **הגדרות חומרה בתחנה:** הגדרת קונפיגורציה מתבצעת תחת לשונית "ניהול תחנות" לפי קבצי `station.prm`. אזור הזמן חייב תמיד להיות מוגדר על ירושלים.
* **עבודה באופליין (Maximus):** במקרה שתחנה מנותקת מהרשת (Offline), העסקאות נשמרות בקובץ `offlinedata.rec` בתיקיית `maximus` ויעלו לפיול כשהתקשורת תחזור. קובץ ההרצה הוא `usa_xml.exe`.
* **סרביס ייצוא נתונים (Export Service):** הסרביס נועד לייצא נתוני CSV ל-FuelPoint דרך הכתובת `https://fueldirectapi.azurewebsites.net`.
* **תקלות בייצוא:** כדי שהמערכת תקרא נכון שמות בעברית, קובץ ה-CSV חייב להישמר בקידוד UTF-8. במקרה של שדרוג הסרביס, חובה לגבות תחילה את קובץ הקונפיגורציה `FuelPointExportService.exe.config`, לעצור את הסרביס הקיים דרך `services.msc`, ורק אז להסיר ולהתקין מחדש. המשתמש שמריץ את ההתקנה חייב להיות בעל הרשאות אדמין (Administrator).

---

## 2. מכשירים ניידים והסבת קופה לפוס נייד (NOVA55F)
כאשר נדרשת תמיכה במכשירי NOVA55F (פוס נייד או הסבה מקופה ניידת), יש לוודא עמידה בדרישות הקדם ובהגדרות התקשורת.

* **דרישות קדם חובה לתחנה:** גרסת RFC 6.2.20.42 ומעלה, פורטים 8082 ו-9090 פתוחים, ו-Dotnet 4.8 ומעלה מותקן (נתמך רק ב-Windows 1607 ומעלה).
* **הגדרת APN ורשת:** הסיסמה לכניסה להגדרות המכשיר היא `260089`. יש להגדיר APN חדש בשם `Roseman` עם הכתובת `Roseman.co.il` ולוודא שהתקבל IP תקין.
* **תהליך הסבה תוכנתי:** יש למחוק את אפליקציית `fuelapp` (ו-`ECR` במידה וקיימת). דרך אתר נייקס/STORM יש לשדרג את גרסת Agamento ל-82 או 86, להוריד את אפליקציית Print bridge, ולהתקין Firmware מסוג `Nayax_047_user` תחת חיבור WIFI.
* **הגדרות מערכת RFC בתחנה:** בקובץ `Dda.prm` יש להגדיר את הפוס הנייד תחת סוג משאבה 55 כדי להבדילו משאר הסוגים. בקובץ `station.json` יש לוודא שהפרמטר `Model` מוגדר כ-`VPOS_MEDIA`, להזין את ה-IP של המכשיר, ולהוסיף `"printer" : "nova"` כדי שההדפסה תעבוד.
* **אורך קבלה:** אם הקבלה נחתכת בסוף ההדפסה, יש להוסיף בקובץ `param.json` את הערך `"NAYAX_NOVA" : 10` להארכת השורות.
* **אקטיבציה:** פרטי ההתחברות לאקטיבציה (הקמת מסוף באפליקציית Agamento) הם שם משתמש `haim-tech` וסיסמה `Hh123456`.

---

## 3. ניהול ותמיכה במערכת Nayax Core (DCS)

* **הקמת חברות ומסופי שב"א:** בעת הקמת חברה, יש לבחור בנתיב סליקה `Ashrait Retail Tandem Direct Roseman`. אם מדובר בחברה עם מספר תחנות, יש לרשום מספר פיקטיבי (לדוגמה 1234) בשדה `SHVA Terminal ID` ברמת החברה, ואת מספרי המסוף האמיתיים יש להגדיר בנפרד תחת כל תחנה.
* **הגדרות מודל לפוס הנייד:** יש להגדיר את ה-Machine Model כ-`Nexo - Fujian LANDI - NOVA Agamento Roseman EMV`.
* **תקלות תצוגה במכשיר:** בהגדרות Agamento Config ב-DCS, יש לוודא שהשדה `Cloud Logo Name` מכיל רווח אחד בלבד (ולא נשאר ריק לגמרי), ויש לכבות/להדליק את הצגת כפתור הביטול לפי הצורך.

---

## 4. תמיכה ושדרוג גרסאות (RFC ו-DDA2)

* **שדרוג חומרת DDA2 לגרסה 2.41:** הצריבה מתבצעת בתקשורת אופטית/TCP. יש לטעון שלושה קבצים לפי הסדר (1 ו-2 הם קבצי BOOT, ו-3 הוא העדכון עצמו). הודעת השגיאה "String not found" שקופצת בטעינת קובץ מספר 2 היא מגבלת מערכת תקינה; יש ללחוץ OK ולהמשיך.
* **התקנת שדרוג RFC:** יש להעביר את הגרסה לתיקיית `c:\temp` ולהריץ את הקובץ `Install.bat`. ההתקנה מבצעת גיבוי אוטומטי של מחיצת ה-DDA לאותה תיקייה.

---

## 5. מועדון לקוחות דור אלון (מגרסה 6.2.20.45)
במידה ויש תקלות זיהוי לקוחות בתחנות דור אלון/אלונית/AM:PM, יש להכיר את המאפיינים הבאים:

* **מגבלות חומרה:** המועדון נתמך אך ורק במכשירי EMV, ואינו נתמך כלל במסופי POS שחור או טרמינלים ללא PIN PAD.
* **הפעלת המועדון:** כדי שהמועדון יפעל, פרמטר `dor-club-2026=Y` חייב להיות מוגדר במקביל גם בקובץ `pos.prm` וגם בקובץ `dda.prm`.
* **חוקיות מספרי טלפון:** זיהוי לקוח מתבצע באמצעות מספר טלפון. על המספר להיות באורך של 9 או 10 ספרות, וחייב להתחיל בקידומת חוקית (02 עד 09). המערכת לא תאפשר הקלדת מספר המתחיל ב-00 או 01 או קצר מ-9 ספרות, ולא תשלח פנייה לשרת על מספרים אלו.
* **הגדרות תצוגה:** כברירת מחדל, זמן הצגת ההודעות במסך הוא 5 שניות (ניתן לשינוי במכשירי Castles דרך `dor-club-2026-message-duration-sec=10`, אך נעול על 5 שניות ב-POS-EMV ו-NOVA). מספר הניסיונות החוזרים מוגדר כברירת מחדל לניסיון אחד נוסף דרך פרמטר `dor-club-2026-retries` בקובץ `pos.prm`.
* **דיבאג (לוגים ותקלות):** למערכת התווספה מסלקה חדשה בתהליך מספר 54 (`DorClub2026_CH`). הלוגים נשמרים תחת השם `dorClub2026_ddmmyy.log`. לאיתור תקלות תקשורת מול השרת ברזולוציה גבוהה יותר, ניתן לשנות בקובץ `DorClub2026_Ch.ini` את הערך של `log level` מ-3 ל-4, מה שיציג מידע על הפניות והתשובות משרת המועדון.
